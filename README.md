# encrypter
In the GUI version the encoded/decoded string automatically gets copied into your clipboard.
In the terminal version you have to copy it manually.

`main.py` is used for the terminal-based app.
`gui.py` is obviously used for the GUI-based app.
`func.py` has the key to encrypt and decrypt.

Ty for using this app!
